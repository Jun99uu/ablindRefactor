//로그인 페이지를 감싸는 컴포넌트이다.

import LoginPageWrapper from "../components/Login/LoginPageWrapper";

const Login = () => {
  return (
    <>
      <LoginPageWrapper />
    </>
  );
};

export default Login;
